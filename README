# EMET2007 Course Website

This repository contains course materials for EMET2007/4007/6007 (Econometric Methods), published as
a static website via GitHub Pages.

**Live site:** https://juergenmeinecke.github.io/EMET2007/

## Repository Structure

```
EMET2007/
├── docs/                   # Sphinx documentation source (RST files)
├── lecture_notes/          # Public lecture PDFs
├── computer_labs/          # Public Jupyter notebooks (student-facing)
├── assignments/            # Public assignment notebooks (student-facing)
├── datasets/               # CSV datasets and descriptions
├── underhood/              # Protected instructor materials (solutions, source files)
└── .github/workflows/      # GitHub Actions CI/CD pipeline
```

The `underhood/` directory contains master copies and solutions. Public directories are synced from there.

## Publishing Workflow

### Automated (GitHub Actions)

Every push to `main` triggers the following pipeline (`.github/workflows/pages.yml`):

1. **Sync assets** - rsync copies public materials into `docs/`
2. **Install dependencies** - Sphinx and sphinx-rtd-theme
3. **Build HTML** - `sphinx-build -b html docs docs/_build/html`
4. **Deploy** - Upload to GitHub Pages

The site typically updates within a few minutes of pushing.

### Local Build

To build locally:

```bash
# Install dependencies
pip install -r docs/requirements.txt

# Build (syncs assets and runs Sphinx)
make html
```

Output is written to `html_output/`.

### Syncing Materials

To sync instructor materials to public directories:

```bash
make sync-public
```

This copies from `underhood/` to the root-level public directories.

## Editing Content

- **Website pages:** Edit RST files in `docs/` (index.rst, lectures.rst, workshops.rst, etc.)
- **Lecture slides:** Update PDFs in `underhood/lecture_notes/weekly_slides/`, then run `make sync-public`
- **Notebooks:** Edit in `underhood/computer_labs/Python/student_facing/` or `underhood/assignments/Python/student_facing/`, then sync
- **Datasets:** Add/modify in `underhood/datasets/`, then sync

## Key Configuration Files

| File | Purpose |
|------|---------|
| `docs/conf.py` | Sphinx configuration (theme, extensions, project metadata) |
| `docs/requirements.txt` | Python dependencies for building docs |
| `.github/workflows/pages.yml` | GitHub Actions deployment pipeline |
| `Makefile` | Local build commands |
| `environment.yml` | Conda environment for running notebooks |

## Technology Stack

- **Documentation:** Sphinx with Read the Docs theme
- **Math rendering:** MathJax
- **CI/CD:** GitHub Actions
- **Hosting:** GitHub Pages
- **Notebooks:** Jupyter (Python 3.11)
